import React, { useEffect } from "react";

const PageWrapper = ({ children }) => {
	
	// console.debug("Size", document.documentElement.scrollHeight, document.body.scrollHeight, window.innerHeight);
	
	return children;
};

export default PageWrapper;